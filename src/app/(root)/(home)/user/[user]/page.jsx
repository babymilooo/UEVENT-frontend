import React from 'react';
import Render from './render';

const page = () => {
    return (
        <div className='w-full h-full'>
            <Render />
        </div>
    );
};

export default page;